プライマリ側の設定

C:\Program Files\PostgreSQL\13\data

1.postgresql.conf
postgresql.conf は、PostgreSQL の基本的な設定ファイルです。
・listen_addresses : listen状態にするIPアドレスを指定
・max_wal_senders : WAL senderプロセス数を定義。スタンバイサーバ以上にする必要がある
・synchronous_standby_names : スタンバイサーバのapplication_nameを定

listen_addresses = '*'
max_wal_senders = 10
synchronous_standby_names = 'standby'

2.pg_hba.conf
pg_hba.confは、PostgreSQL に接続するクライアントの認証に関する設定ファイルです。
このファイルで、スタンバイサーバからの接続を許可します。
・host    replication     all             [スタンバイ側のIPアドレス/**]           trust

host    replication     all             0.0.0.0/0               scram-sha-256

サービス再起動
net start postgresql-x64-13
net stop postgresql-x64-13

'0.0.0.0' の状態を確認します。
netstat -ano -o | find "5432"


スタンバイ側の設定
1.postgresql.auto.conf
このファイルの一番後ろに「application_name=プライマリで設定したsynchronous_standby_names」を記載

# Do not edit this file manually!
# It will be overwritten by the ALTER SYSTEM command.
primary_conninfo = 'user=postgres passfile=''/var/lib/pgsql/.pgpass'' channel_binding=prefer host=127.0.0.1 port=5432 sslmode=prefer sslcompression=0 sslsni=1 ssl_min_protocol_version=TLSv1.2 gssencmode=prefer krbsrvname=postgres target_session_attrs=any application_name=standby'


2.postgresql.conf
pg_basebackup の実行後は、プライマリサーバーの設定がスタンバイ側に反映されてしまうため、プライマリ側の未設定が必要な個所を以下のようにコメントアウトします。
#listen_addresses = '0.0.0.0'
#max_wal_senders = 10
#synchronous_standby_names = 'standby'

select * from pg_stat_replication;

------------------------------------------------------------------------------------------
クラスタの作成
1台のPCで複数のPostgreSQLサーバ構築

firewall.cpl
新しい規則⇒ポート⇒TCP⇒特定のローカルポート⇒5432,5433,5434⇒接続を許可する⇒PostgreSQL
5432,5433,5434

initdb -U postgres -D <新規フォルダパス>

データベースクラスタの作成
C:\data\cluster_a
initdb -U postgres -D C:\data\cluster_a

[cluster_1/postgresql.conf]
port = 5433

PostgreSQLインスタンスの起動
pg_ctl -D C:\data\cluster_a -l C:\data\cluster_a\cluster_a.log start
netstat -ano -o | find "5432"
netstat -ano -o | find "5433"

データベースとテーブルの作成
psql -h 127.0.0.1 -p 5432 -U postgres
create database hoge;

psql -U postgres -p 5433
create database hoge;

------------------------------------------------------------------------------------------
ロジカルレプリケーション

<master side>

1. edit
[data/postgresql.conf]
wal_level = logical

[data/pg_hba.conf]


2. make publication
psql -h localhost -p 5432 -U postgres
create database hoge;
\l
\c hoge
create table table_a(
    user_id uuid not null default gen_random_uuid(),
    user_name varchar not null,
    user_level integer not null,
    primary key (user_id)
);
\dt

#パブリケーション作成時にレプリケーション対象テーブルと操作を指定 ※今回は登録と更新を同期し、削除を同期しない設定
CREATE PUBLICATION test_pub FOR TABLE table_a WITH (publish = 'insert,update');

!FOR ALL TABLES
!CREATE PUBLICATION pub_a FOR TABLE table_a WITH (publish = 'insert, update, delete, truncate');

#レプリケーション対象テーブルの確認
SELECT * FROM pg_publication_tables;

#レプリケーション対象操作の確認
SELECT * FROM pg_publication;

<standby side>

1. make subscriction
psql -p 5433 -U postgres
create database hoge;
\l
\c hoge
create table table_a(
    user_id uuid not null default gen_random_uuid(),
    user_name varchar not null,
    user_level integer not null,
    primary key (user_id)
);
\dt

※サブスクリプション作成時にパブリッシャーへの接続情報とパブリケーションを指定
CREATE SUBSCRIPTION test_sub CONNECTION 'host=localhost port=5432 dbname=hoge password=hoge' PUBLICATION test_pub;

\encoding
\encoding UTF-8
\encoding SJIS

SELECT * FROM pg_subscription; 

<check!!!>

<pub>
select * from table_a;
insert into table_a (user_name, user_level) values
('kenta', 32),
('wataru', 56);

insert into table_a (user_name, user_level) values
('piyo', 12);

insert into table_a (user_name, user_level) values
('hoge', 23);

<sub>
select * from table_a;
------------------------------------------------------------------------------------------

<VIEW>

create view user_list as select user_id, user_name from table_a;
select * from user_list;

create view user_level_avg as select avg(user_level) from table_a;
select * from user_level_avg;

select avg(user_level) from table_a;
select max(user_level), min(user_level) from table_a; 

------------------------------------------------------------------------------------------

<PROC>

create table items(
    user_id uuid not null,
    item_name varchar not null,
    item_level integer not null,
    foreign key (user_id) references table_a(user_id)
);

CREATE PROCEDURE add_item(user_id uuid, item_name varchar, item_level integer)
LANGUAGE SQL
AS $$
INSERT INTO items VALUES (user_id, item_name, item_level);
$$;

CALL add_item('9c398f32-e088-4b0a-bf6e-b4a623b516ed', '肉まん', 14);

select * from items;